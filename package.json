{
  "name": "vacancy-location-search",
  "version": "1.0.0",
  "description": "Zoek vacatures op locatie met coördinaten-cascade (vacature → bedrijf → locatie)",
  "scripts": {
    "start": "block start",
    "build": "block build",
    "release": "block release"
  },
  "dependencies": {
    "@airtable/blocks": "^0.0.54",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "@airtable/blocks-cli": "^2.0.0",
    "@airtable/blocks-webpack-bundler": "^0.0.3",
    "@types/react": "^16.14.0"
  }
}
